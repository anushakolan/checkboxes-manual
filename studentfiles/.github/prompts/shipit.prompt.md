---
agent: agent
description: Commit and push changes with a meaningful message
---

Analyze staged/unstaged changes, generate a descriptive commit message, show it for approval, then commit and push (or just commit if no remote).
